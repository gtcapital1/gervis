# Installa PostgreSQL
sudo yum install -y postgresql # Installa PostgreSQLtgresql-devel postgresql-consudo yum install -y postgresql postgresql-server postgresql-devel postgresql-contrib
# Inizializza il database
# Inizializza il databasenitdb
sudo service postgresql initdb
# Avvia il servizio
# Avvia il servizioesql start
sudo service postgresql start
# Configura per l'avvio automatico
# Configura per l'avvio automatico
sudo chkconfig postgresql on
# Modifica pg_hba.conf per permettere autenticazione password
# Modifica pg_hba.conf per permettere autenticazione password/pg_hba.conf.bak
sudo cp /var/lib/pgsql9/data/pg_hba.conf /var/lib/pgsql9/data/pg_hba.conf.bak
sudo sed -i 's/ident/md5/g' /var/lib/pgsql9/data/pg_hba.conf
sudo sed -i 's/peer/md5/g' /var/lib/pgsql9/data/pg_hba.conf
# Riavvia PostgreSQL per applicare le modifiche
# Riavvia PostgreSQL per applicare le modifiche
sudo service postgresql restart
# Crea database e utente
# Crea database e utente "CREATE DATABASE gervis;"
sudo -u postgres psql -c "CREATE DATABASE gervis;"PASSWORD 'Oliver1';"
sudo -u postgres psql -c "CREATE USER gervis WITH PASSWORD 'Oliver1';"ervis;"
sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE gervis TO gervis;"
Last metadata expiration check: 0:03:12 ago on Wed Mar 19 14:22:45 2025.
No match for argument: postgresql
No match for argument: postgresql-server
No match for argument: postgresql-contrib
Error: Unable to find a match: postgresql postgresql-server postgresql-contrib
The service command supports only basic LSB actions (start, stop, restart, try-restart, reload, reload-or-restart, try-reload-or-restart, force-reload, status, condrestart). For other actions, please try to use systemctl.
Redirecting to /bin/systemctl start postgresql.service
Failed to start postgresql.service: Unit postgresql.service not found.
error reading information on service postgresql: No such file or directory
cp: cannot stat '/var/lib/pgsql9/data/pg_hba.conf': No such file or directory
sed: can't read /var/lib/pgsql9/data/pg_hba.conf: No such file or directory
sed: can't read /var/lib/pgsql9/data/pg_hba.conf: No such file or directory
Redirecting to /bin/systemctl restart postgresql.service
Failed to restart postgresql.service: Unit postgresql.service not found.
sudo: unknown user postgres
sudo: error initializing audit plugin sudoers_audit
sudo: unknown user postgres
sudo: error initializing audit plugin sudoers_audit
sudo: unknown user postgres
sudo: error initializing audit plugin sudoers_audit
[ec2-user@ip-172-31-36-102 ~]$ # Installa PostgreSQL
sudo yum install -y postgresql # Installa PostgreSQLtgresql-devel postgresql-consudo yum install -y postgresql postgresql-server postgresql-devel postgresql-contrib
